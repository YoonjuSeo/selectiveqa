# Stage E 사전등록 — Kanana-1.5-8B 일반화

작성일 2026-09-09 · 실험 실행 전 커밋

## 1. 목적

가설 (b) "혼입의 부작용은 한국어 특화 모델에서 나타난다"의 현재 근거는 한국어 특화
모델 n = 1(EXAONE-3.5-7.8B)이다. 이 관찰만으로는 다음 두 설명을 구분할 수 없다.

- (b-1) 한국어 특화 학습이 원인이다.
- (b-2) EXAONE 계열 고유의 사전학습·정렬 방식이 원인이다.

Kanana-1.5-8B는 EXAONE과 무관한 계열의 한국어 모델이므로, 이 실험은 두 설명을
가르는 네 번째 데이터 포인트다. 붕괴가 재현되면 (b-1) 쪽으로, 무손실이면 (b-2) 쪽으로
기운다.

부수적으로, Kanana는 Llama 아키텍처 기반이다. Llama-3.1-8B가 이미 무손실로 판정됐으므로
아키텍처를 사실상 고정한 채 한국어 학습만 달라진 대조가 된다. 여기서 붕괴가 나오면
"아키텍처가 아니라 한국어 학습이 원인"이라는 진술의 근거가 강해진다.

단, Kanana는 한국어·영어 bilingual 모델로 EXAONE 같은 한국어 특화와는 결이 다를 수
있다. 결과 해석 시 이 구분을 명시한다.

## 2. 모델

| 항목 | 값 |
| --- | --- |
| 저장소 | `kakaocorp/kanana-1.5-8b-instruct-2505` |
| 리비전 | `<실행 전 커밋 해시로 고정 — config_kanana.yaml에 기록>` |
| 크기 / 정밀도 | 8B params / BF16 |
| 아키텍처 | llama |
| 라이선스 | Apache 2.0 (비gated) |
| 채팅 템플릿 | 저장소 제공 템플릿 사용, `apply_template(enable_thinking=None)` |

`config.json`은 32,768 토큰 이하로 설정되어 있고 그 이상은 YaRN이 필요하나, 본 실험은
`max_seq_len: 2048`이므로 config를 수정하지 않는다.

## 3. 조건

기존 세 모델(EXAONE, Qwen3, Llama)과 동일하다.

- **M0** — 미세조정 없는 원본 모델, 평가 전건 1회
- **M1** — 응답가능 3,000건만으로 QLoRA 학습, 시드 42 / 43 / 44
- **M2-r05** — 응답불가능 예시 5% 혼입(`train_mix_r05.jsonl`), 시드 42 / 43 / 44

평가셋은 `eval_full_v2.jsonl` 1,767건, 제외 30건을 적용한 유효 표본 n = 1,737
(응답가능 1,170 · easy UA 300 · hard UA 267).

## 4. 판정 기준 — 변경 없음

H1~H5는 사전등록(260817)과 부칙(260828)을 그대로 적용한다. 지표 정의, 층화 paired
bootstrap 10,000회, 판정 문턱, H4 주 신호(`m1_conf`) 모두 변경하지 않는다.

- **H1** easy·hard 각각 M2 환각률 95% CI 상한 < M1 CI 하한, 3/3 시드
- **H2** 응답가능 전체 EM 차이(M2−M1) 95% CI 하한 > −0.03, 3/3 시드
- **H3** M1의 hard UA 환각 vs 응답가능 정답 분리 AUROC CI 하한 > 0.85, 3/3 시드
- **H4** M2의 UA 내 무응답 결정 예측 AUROC CI 하한 > 0.75, 3/3 시드
- **H5** M2의 전 유형 ΔECE(대 M0) 95% CI 상한 < 0, 3/3 시드

risk-coverage 비교(M1+필터 vs M2 vs M2+필터)와 사전 지정 운영점 2개
(coverage ≥ 0.9에서 최소 risk / risk ≤ 0.05에서 최대 coverage)도 동일하게 산출한다.

## 5. 실행 관문 (gate)

**M0에서 easy UA 무응답률이 90% 미만이면 Stage E를 중단하고 그 사실만 보고한다.**

근거: 본 연구의 출발점은 "원본 모델은 답 없는 질문에 무응답하지만 미세조정 후
그 행동이 소거된다"는 관찰이다. M0의 easy UA 무응답률이 낮으면 소거를 관찰할 기저가
없어 실험 설계 자체가 성립하지 않는다. 기존 세 모델은 EXAONE 100%, Qwen3 100%,
Llama 97.3%였고, 90%는 Llama보다 낮은 값을 여유로 잡은 것이다.

이 관문을 결과 확인 전에 명시하는 이유는, 사후에 "이 모델은 부적합했다"고 판단하면
결과에 따른 선택적 배제가 되기 때문이다. M0 판정 결과는 통과 여부와 무관하게 보고한다.

## 6. 실행하지 않는 것

**혼입률 격자(r10 · r30)는 실행하지 않는다.** 세 모델에서 이미 답이 나왔다 —
EXAONE은 스위치 3/3(강건), Qwen3·Llama는 조건부 스위치이며 시드별 부호가 갈렸다
(3.12절). 혼입량이 hard UA 회수를 통제하는 다이얼로 작동하지 않는다는 결론이
반복 확인된 상태이므로, 네 번째 모델에서 격자를 다시 도는 것은 학습 예산 대비
정보량이 낮다.

**에폭 조기종료 분석과 검색 불일치형 UA는 선택 사항으로 둔다.** 핵심 판정(H1~H5)이
끝난 뒤 여유가 있으면 수행하되, 하지 않아도 Stage E의 목적은 달성된다.

## 7. 파이프라인 변경 범위

코드 변경은 없다. 추가되는 것은 다음뿐이다.

1. `config_kanana.yaml` 신규 — `base_model`, `revision`, `results_dir`, `adapter_dir`만
   기존 config에서 변경. 학습 하이퍼파라미터는 Qwen3와 동일하게 맞춘다
   (`batch_size: 1`, `grad_accum: 16`, 실효 배치 16, `epochs: 2`, LoRA r=16/α=32).
   `train.seeds`는 리스트 `[42, 43, 44]`로 표기한다.
2. `modal_train.py` · `modal_inference.py` · `modal_eval.py`에 `add_local_file`
   한 줄씩 추가(컨테이너에 config 전달).
3. 볼륨 `selectiveqa-results`의 `kanana/` 아래에 제외 목록 2개 업로드.

프롬프트, 타깃 형식, 채점 로직, 판정 코드(`evaluate_followup.py`), 격자 비교 코드
(`compare_ratio.py`)는 손대지 않는다.

## 8. 예상 결과와 해석

| 결과 | 해석 |
| --- | --- |
| 붕괴형 (H2·H5 미충족, ΔEM 대폭 하락, 토큰 배율 증가, 오무응답 발생) | 가설 (b) 2/2 지지. 아키텍처가 Llama임에도 붕괴한다면 한국어 학습이 원인이라는 진술이 강해진다. |
| 무손실형 (H1·H2·H5 채택, ΔEM ±수%p) | 가설 (b) 약화. 붕괴는 EXAONE 계열 고유 현상으로 좁혀지며, 한국어 특화 자체를 원인으로 지목할 수 없게 된다. |

어느 쪽이든 보고한다. 무손실형이 나오면 본문 3.11절의 가설 (b) 지지 진술을 수정하고,
5장 한계에 "한국어 특화 모델 n=2 중 1개만 붕괴"로 명기한다.

## 9. 기록

- 본 문서는 Kanana의 M0 추론을 포함해 어떤 결과도 확인하기 전에 작성·커밋한다.
- H1~H5 기준을 그대로 적용하므로 새로 정하는 판정 문턱은 없다.
- 제외 문항 수는 30건이다. 기존 본문의 "34건" 표기는 판정 스크립트의 제외 목록 수집
  함수가 메타데이터 문자열 4건을 함께 집계한 데 따른 오기이며, 실제 제외는 30건으로
  전 모델·전 조건에서 동일하다(n = 1,737 = 1,767 − 30). 수집 함수를 `question_ids`
  필드만 읽도록 좁히는 수정은 판정 결과를 바꾸지 않으나 Stage E 종료 후 별도 커밋으로
  처리한다.
